// api.js

import axios from 'axios';

const apiUrl = 'http://localhost:8087/music/';

const api = axios.create({
    baseURL: apiUrl,
});

export const getCDList = () => api.get('/cds');

export default api;
